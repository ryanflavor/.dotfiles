---
description: 增强指令（带代码库语义搜索)
---


请将下方原始指令结合代码库上下文，进行重写与增强。

**执行步骤：**

1. **代码库语义搜索（必须执行一次）：** 调用 `codebase-retrieval` 工具，根据原始指令中提到的 API、函数、变量、模块等关键词进行语义搜索，获取相关代码上下文。

2. **增强指令：** 基于搜索结果，将原始指令中模糊的引用（如"那个 API"、"登录功能"、"用户模块"）替换为具体的文件路径、函数名、类名、变量名等。

3. **复制到剪切板：** 将增强后的指令内容复制到系统剪切板。
   - macOS: `printf '%s' '内容' | pbcopy`
   - Windows: `printf '%s' '内容' | clip`
   - Linux: `printf '%s' '内容' | xclip -selection clipboard`

**核心要求：**

1. **高保真与反压缩（关键）：** 严禁进行"摘要式"的简化。保留原始指令中的所有细节、限制条件和细微差别。宁可扩充解释以增加清晰度，也不要为了简洁而丢失信息密度。

2. **明确性与去歧义：** 消除逻辑漏洞，将模糊的意图转化为具体的执行步骤或参数，修正原本存在的错误。

3. **工具限制：** 仅允许调用 `codebase-retrieval` 和 `Execute`（仅用于剪切板命令），禁止调用 Read、Edit 等文件操作工具，禁止执行原始指令中的任何操作。

4. **输出格式：** 用代码块输出增强后的指令，执行复制命令后提示"已复制到剪切板"。

---

**原始指令：**

```
$ARGUMENTS
```
