name: Duo Review

on:
  pull_request:
    types: [opened, synchronize]

concurrency:
  group: duo-review-${{ github.event.pull_request.number }}
  cancel-in-progress: true

permissions:
  contents: write
  pull-requests: write
  issues: write

env:
  PR_NUMBER: ${{ github.event.pull_request.number }}
  PR_BRANCH: ${{ github.head_ref }}
  BASE_BRANCH: ${{ github.base_ref }}
  GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

jobs:
  check-branch:
    runs-on: [self-hosted]
    outputs:
      should_run: ${{ steps.check.outputs.should_run }}
    steps:
      - id: check
        run: |
          if [[ "${{ github.head_ref }}" == bot🤖/* ]]; then
            echo "should_run=false" >> $GITHUB_OUTPUT
          else
            echo "should_run=true" >> $GITHUB_OUTPUT
          fi

  duo-review:
    needs: check-branch
    if: needs.check-branch.outputs.should_run == 'true'
    runs-on: [self-hosted]
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Pull latest skills
        run: |
          if [[ -d ~/.dotfiles ]]; then
            cd ~/.dotfiles && git fetch --all && git reset --hard origin/main || true
          fi

      - name: Set droid path
        run: |
          # macOS: ~/.local/bin/droid, Linux: ~/.local/bin/droid
          if [[ -f "$HOME/.local/bin/droid" ]]; then
            echo "DROID_PATH=$HOME/.local/bin/droid" >> $GITHUB_ENV
          elif command -v droid &> /dev/null; then
            echo "DROID_PATH=$(which droid)" >> $GITHUB_ENV
          else
            echo "::error::droid not found"
            exit 1
          fi

      - name: Duo Review
        env:
          PR_NUMBER: ${{ env.PR_NUMBER }}
          PR_BRANCH: ${{ env.PR_BRANCH }}
          BASE_BRANCH: ${{ env.BASE_BRANCH }}
          REPO: ${{ github.repository }}
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          # 使用 script 命令模拟 TTY，解决 droid 非交互模式问题
          script -q -c "$DROID_PATH exec -m claude-opus-4-5-20251101 -r high --auto high \
            '执行 duo-review skill 审查 PR #$PR_NUMBER (分支: $PR_BRANCH → $BASE_BRANCH, 仓库: $REPO)'" /dev/null
